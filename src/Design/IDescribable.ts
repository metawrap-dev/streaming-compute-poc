/**
 * Something that can be described to something.
 * 
 * @todo This is a placeholder for a more complex interface that will be the visitor class.
 *
 * @author James McParlane
 * @interface
 */
export interface IDescribable {
  /**
   * Describe the element as a string.
   * @returns {string}
   */
  toString(): string
}
